schema_version: "1.0"
workflow:
  id: "ddd-full-cycle"
  name: "DDD + Clean Architecture Full Cycle"
  version: "2.0.0"
  author: "your-org"
  description: >
    Full spec-driven development cycle with DDD and Clean Architecture.
    All project context is read from project-brief.md — no CLI duplication.
    Phases: clarify → spec → plan A (§1-10) → plan B (§11-14) →
    plan C1 (§15-16) → plan C2 (§17-20 + conventions) → plan C3a (api-contract)
    → plan C3b (data-model) → plan C3c (interfaces) → plan C3d (gitignore+CLAUDE+ci)
    → tasks (with integration tasks) → validate dependency graph → implement loop
    (batched parallel tasks, 11 checks, TDD, rollback) → adaptive retrospective → final verify.

requires:
  speckit_version: ">=0.7.2"
  integrations:
    any: ["claude"]

# No inputs — all project context comes from project-brief.md.
# Fill in project-brief.md before running this workflow.
inputs: {}

steps:

  # ── PREFLIGHT: VALIDATE project-brief.md ──────────────────────
  - id: preflight
    type: prompt
    integration: claude
    input:
      args: >
        Read project-brief.md.
        Check that ALL of the following fields are filled in
        (not placeholder text in brackets):
          - Project name
          - What does this system do (at least 2 sentences)
          - Stack: backend_language, backend_framework, database,
                   frontend_language, frontend_framework, monorepo_tool
          - Complexity: simple | medium | complex
          - At least one hard constraint or explicit "none"
          - At least one out-of-scope item or explicit "none"
          - Security posture

        If any field is missing or still contains placeholder text:
          List the missing fields and stop.
          Print: "Fill in project-brief.md and run the workflow again."

        If all fields are present:
          Print a one-paragraph summary of what you read.

        Also check the following tooling is installed locally:
          - gitleaks: run 'gitleaks version' — needed for secret scanning
          - node: run 'node --version' — needed for Playwright MCP

        If gitleaks is missing: print a warning suggesting
          'bash scripts/setup-hooks.sh' to install it.
        If node is missing: print a warning suggesting
          'bash scripts/setup-mcp.sh' after installing Node.js.

        Print: "project-brief.md is complete — proceeding."

  - id: review-preflight
    type: gate
    message: |
      Review the project-brief.md summary above.
      Does it correctly reflect your project?
      If anything is wrong, edit project-brief.md and restart the workflow.
    options: [approve, restart]
    on_restart: abort

  # ── PHASE 1: CLARIFY ──────────────────────────────────────────
  - id: clarify
    type: prompt
    integration: claude
    input:
      args: >
        Read project-brief.md fully.

        Based on what you read, ask 3-5 targeted clarifying questions.
        Focus only on things that are genuinely ambiguous or missing and
        that would significantly affect architecture decisions.
        Do not ask about things already covered in the brief.
        Do not ask generic questions.

        Good questions probe: edge cases in the core domain, conflict
        resolution between competing requirements, specific compliance
        obligations, external system API constraints, and expected
        data volumes for the most critical operations.

  - id: review-clarify
    type: gate
    message: |
      Answer the clarifying questions above.
      Edit your answers directly in this gate if needed.
      Approve when requirements are well-understood.
    options: [approve, re-clarify]
    on_re-clarify:
      goto: clarify

  # ── PHASE 2: SPEC ─────────────────────────────────────────────
  - id: specify
    command: speckit.specify
    integration: claude
    input:
      args: >
        Read project-brief.md and the clarification answers from the
        previous phase. Produce the feature spec (spec.md).

  - id: review-spec
    type: gate
    message: |
      Review .specify/specs/[feature]/spec.md
      Does it correctly capture user stories and acceptance requirements?
      Are there missing scenarios or incorrect assumptions?
      Approve to proceed to the technical plan.
    options: [approve, reject]
    on_reject: abort

  # ── PHASE 3A: PLAN — DOMAIN + ARCHITECTURE (§1–§10) ──────────
  # Split into two passes to avoid context window overflow.
  # Pass A covers the decisions that affect everything else.
  - id: plan-part-a
    command: speckit.plan
    integration: claude
    input:
      args: >
        Read project-brief.md and .specify/specs/[feature]/spec.md.
        Use ddd-clean-arch/templates/plan-template.md.

        Produce ONLY sections §1 through §10 of plan.md:
          §1  Requirements
          §2  Ubiquitous language
          §3  Bounded contexts
          §4  Aggregates (full detail including concurrency and idempotency)
          §5  Domain services
          §6  Architecture (modules, dependency graph, layer rules)
          §7  Cross-cutting concerns (error taxonomy, transaction boundary,
              authorization, logging)
          §8  API design (all endpoints with full detail)
          §9  Security (threat model, CSP consistent with §8 token delivery
              and CORS)
          §10 Performance & scalability (end-to-end budget first, then
              backend p95 derived from it)

        Do NOT produce §11 onwards yet.
        Do NOT produce companion files yet.
        End with: "Plan Part A complete (§1–§10). Waiting for review."

  - id: review-plan-a
    type: gate
    message: |
      ━━ REVIEW PLAN PART A (§1–§10) ━━

      [ ] §2 Ubiquitous language: exact names you want in code?
      [ ] §4 Aggregates: invariants correct? Concurrency strategy right per aggregate?
      [ ] §4 Aggregates: idempotency mechanisms specified?
      [ ] §7 Error taxonomy: four types defined? Envelope shape correct?
      [ ] §8 API: every endpoint present? Auth correct on each?
      [ ] §8 Token delivery: security trade-off acceptable?
      [ ] §8 CORS: consistent with security requirements?
      [ ] §9 CSP: connect-src includes API origin?
      [ ] §9 CSP: consistent with token delivery (cookie domain if httpOnly)?
      [ ] §10 End-to-end budget: realistic? Backend p95 derived from it?

      PREVIEW — §13 api_testing_tool (decided in Plan B, but depends on §8):
        Based on the backend_language in project-brief.md, the API testing tool
        will be selected in Plan B as:
          Java        → REST Assured  (runs via mvn test / gradle test)
          TypeScript  → Playwright API (runs via npx playwright test --project=api)
          Python      → pytest + httpx (runs via pytest tests/api/)
        If this is not the right tool for your project, note it now so Plan B
        uses the correct selection.

      Edit plan.md directly before approving.
      These decisions propagate through everything that follows.
    options: [approve, revise]
    on_revise:
      goto: plan-part-a

  # ── PHASE 3B: PLAN — RELIABILITY + DATA + TESTING + DEVEX (§11–§14)
  # ── PHASE 3B: §11–§14 (reliability, data, testing, devex) ───────
  # Split from the original monolithic pass to avoid context window degradation.
  # §11-14 are dense structural decisions that reference §1-10 heavily.
  # §15-20 + companion files come in phase 3C after these are reviewed.
  - id: plan-part-b
    command: speckit.plan
    integration: claude
    input:
      args: >
        §1–§10 of plan.md are approved. Produce ONLY §11–§14 now.
        All decisions must be consistent with §1–§10.
        Call out any tension explicitly rather than silently contradicting
        an earlier decision. Do NOT produce companion files yet.

        Produce:
          §11 Reliability & observability
              Correlation ID header name must match §8 correlation_id_header_name exactly.
              Health check paths must not overlap with API endpoint paths from §8.
              Circuit breakers only for external deps listed in §1 requirements.

          §12 Data design
              Tables must reflect §4 aggregate boundaries — one table per aggregate root
              unless denormalization is explicitly justified.
              Indexes must each name the specific use case or query they support.
              Migration strategy: include teardown scripts for test database setup.

          §13 Testing strategy
              - api_testing_tool: select based on backend_language
                  Java → rest-assured (runner: mvn test or gradle test)
                  TypeScript → playwright-api (runner: npx playwright test --project=api)
                  Python → pytest-httpx (runner: pytest tests/api/)
              - e2e_testing_tool: playwright (always)
              - Define all three regression commands:
                  regression_command.all (arch + unit + integration + api + e2e)
                  regression_command.api_only (arch + unit + integration + api, no browser)
                  regression_command.e2e_only (browser tests only, headless)
              - e2e_tests.coverage_focus: list every critical user journey from spec.md
                  Form: "As a [user], I [action sequence] and verify [outcome]"
              - e2e_tests.location and naming_convention must be set
              - test_data_strategy:
                  approach: [test_containers_per_test | shared_db_with_cleanup | factory_functions]
                  factory_location: [path — e.g. src/test/java/fixtures/ or tests/fixtures/]
              - e2e_data_setup:
                  strategy: [api_seeding | db_seeding | before_each_hook]
                  cleanup: [after_each | after_all | none_if_isolated_db]
                  isolation_guarantee: [each_test_independent | run_in_isolation_only]
              - e2e_data_setup.strategy: [how E2E tests set up and tear down their data]
              - flaky_test_protocol:
                  quarantine_label: [label or annotation to mark flaky tests]
                  max_time_in_quarantine: [e.g. "2 weeks" — after this, fix or delete]
                  quarantine_location: [separate file or tag]
              - E2E test owner and which CI pipeline runs them

          §14 Monorepo & developer experience
              Frontend cache invalidation must align with §8 cache-control + §10 caching.
              local_dev_setup.steps must be runnable by a new engineer in under 15 minutes.
              Include: ci_local_script: scripts/ci-local.sh (to be created in Phase 3C).

        End with: "Plan Part B (§11–§14) complete. Waiting for review."

  - id: review-plan-b
    type: gate
    message: |
      ━━ REVIEW PLAN PART B (§11–§14) ━━

      §11 Reliability:
        [ ] Correlation ID header name matches §8 exactly?
        [ ] Health check paths don't conflict with API routes?

      §12 Data design:
        [ ] Tables match §4 aggregates? Indexes each name their supporting use case?
        [ ] Migration teardown scripts included for test DB setup?

      §13 Testing:
        [ ] api_testing_tool selected correctly for the backend language?
        [ ] All three regression_command variants defined (all / api_only / e2e_only)?
        [ ] e2e_tests.coverage_focus: every critical user journey listed?
         [ ] e2e_data_setup.strategy defined?
        [ ] flaky_test_protocol.max_time_in_quarantine set?
        [ ] E2E test owner and pipeline decided?

      §14 DevEx:
        [ ] local_dev_setup.steps realistic and complete?
        [ ] ci_local_script: scripts/ci-local.sh referenced in §14?

      Edit plan.md directly before approving.
    options: [approve, revise]
    on_revise:
      goto: plan-part-b

  # ── PHASE 3C1: §15–§16 (edge cases + constraints) ─────────────
  # Fast, low-risk — domain-specific edge cases and hard constraints.
  - id: plan-part-c1
    command: speckit.plan
    integration: claude
    input:
      args: >
        §1–§14 of plan.md are approved. Produce §15–§16 only.

        §15 Edge cases & failure modes
            Minimum 10 scenarios. Do not include concurrency conflicts (§4 covers those).
            Focus on domain-specific scenarios. For each FAILURE: detection, response, recovery.
            Be specific — "the payment provider returns a 429" not "upstream failure".

        §16 Architectural constraints
            Exactly 10. Specific to THIS codebase — not generic advice.
            Form: "Never [specific action] because [specific consequence in this domain]."

        End with: "Plan Part C1 (§15–§16) complete. Waiting for review."

  - id: review-plan-c1
    type: gate
    message: |
      ━━ REVIEW PLAN PART C1 (§15–§16) ━━

      [ ] §15 Edge cases: 10 specific scenarios with detection/response/recovery?
          Domain-specific (not generic "upstream failure")?
      [ ] §16 Constraints: exactly 10? Each specific to this codebase with consequence?
    options: [approve, revise]
    on_revise:
      goto: plan-part-c1

  # ── PHASE 3C2: §17–§20 + conventions.md ────────────────────────
  # Medium risk — DoD, self-critique, ADRs, arch tests, conventions.
  - id: plan-part-c2
    command: speckit.plan
    integration: claude
    input:
      args: >
        §1–§14 and §15–§16 of plan.md are approved. Produce §17–§20 and
        .specify/memory/conventions.md.

        §17 Definition of done
            Concrete verifiable criteria. Must align with the 11 checks in speckit.implement.
            Include: test file committed, regression suite green, secret scan clean.

        §18 Self-critique (mandatory — do not soften)
            The 3 weakest points. For each: problem, reason accepted, early warning signs,
            what changes under 20x load / second team / key dependency lost.

        §19 ADR log
            One ADR per: aggregate boundary decision, locking strategy, tech stack selection,
            bounded context boundary, token delivery, CORS policy, rendering strategy,
            contract sharing strategy, and any irreversible decision.

        §20 Architectural test inventory
            Generate enforcement test files:
              Java backend → [module]/src/test/java/arch/ArchitectureTest.java (ArchUnit)
              TypeScript backend → [module]/.dependency-cruiser.cjs
              Other → closest available static analysis tool config
              Frontend → [frontend-module]/.dependency-cruiser.cjs
            Run arch tests on empty codebase. Must pass.
            Populate §20 after generating arch test files.

        .specify/memory/conventions.md
            Naming: use cases, repositories, events, value objects, test classes.
            Method conventions: execute signature, factory vs constructor.
            Error handling conventions per layer.
            Frontend: component, hook, type naming. Error surface per error type.
            Shared: commit format, TODO format.
            Decision log section (empty — grows via /speckit.review).

        End with: "Plan Part C2 (§17–§20 + conventions) complete. Waiting for review."

  - id: review-plan-c2
    type: gate
    message: |
      ━━ REVIEW PLAN PART C2 (§17–§20 + conventions) ━━

      [ ] §17 Done criteria: aligned with the 11 implement checks?
      [ ] §18 Self-critique: honest? Early warning signs defined?
      [ ] §19 ADRs: one per aggregate boundary? Per locking strategy? Per stack choice?
      [ ] §20 Arch test inventory: populated after arch tests generated?
      [ ] Arch test files: pass on empty codebase?
      [ ] conventions.md: naming patterns filled in for this stack?
    options: [approve, revise]
    on_revise:
      goto: plan-part-c2

  # ── PHASE 3C3A: API CONTRACT ───────────────────────────────────
  # Critical path — this file is the source of truth for all downstream tasks.
  # If this fails, only C3b/C3c/C3d need revision (not the whole plan).
  - id: plan-part-c3a
    command: speckit.plan
    integration: claude
    input:
      args: >
        §1–§20 of plan.md are approved. Produce ONLY docs/spec/api-contract.yaml.

        OpenAPI 3.1. Error envelope as $component referenced everywhere.
        Every endpoint from §8. CORS + token delivery as top comment block.
        Cache-control response headers for cacheable endpoints.

        End with: "Plan Part C3a (api-contract.yaml) complete. Waiting for review."

  - id: review-plan-c3a
    type: gate
    message: |
      ━━ REVIEW PLAN PART C3a (api-contract.yaml) ━━

      [ ] All §8 endpoints present with correct HTTP methods and paths?
      [ ] Error envelope as $component referenced everywhere?
      [ ] CORS + token delivery documented in top comment block?
      [ ] Cache-control headers on cacheable endpoints?

      If anything is wrong, edit api-contract.yaml and revise.
      This is the source of truth for implementation — get it right.
    options: [approve, revise]
    on_revise:
      goto: plan-part-c3a

  # ── PHASE 3C3B: DATA MODEL ─────────────────────────────────────
  # Independent of C3a — only depends on §12 from plan.md.
  - id: plan-part-c3b
    command: speckit.plan
    integration: claude
    input:
      args: >
        §1–§20 of plan.md are approved. Produce ONLY docs/spec/data-model.sql.

        DDL from §12. Migration strategy as top comment block.
        Each table: comment stating aggregate and bounded context.
        Each index: comment stating which use case it supports.
        Include teardown SQL for test database setup (schema drop + recreate).

        End with: "Plan Part C3b (data-model.sql) complete. Waiting for review."

  - id: review-plan-c3b
    type: gate
    message: |
      ━━ REVIEW PLAN PART C3b (data-model.sql) ━━

      [ ] Schema matches §12 tables and column types?
      [ ] Each table has aggregate + bounded context comment?
      [ ] Each index names its supporting use case?
      [ ] Teardown SQL included for test DB setup?

      Edit data-model.sql and revise if needed.
    options: [approve, revise]
    on_revise:
      goto: plan-part-c3b

  # ── PHASE 3C3C: BACKEND + FRONTEND INTERFACES ──────────────────
  # Depends on C3a (api-contract) — error types and API signatures.
  - id: plan-part-c3c
    command: speckit.plan
    integration: claude
    input:
      args: >
        §1–§20 of plan.md are approved. Produce ONLY:
          docs/spec/backend-interfaces.[ext]
          docs/spec/frontend-interfaces.[ext]

        docs/spec/backend-interfaces.[ext]
          Domain layer signatures only — no implementation bodies.
          Aggregate roots, value objects, domain events, repository interfaces.
          Zero infrastructure imports.

        docs/spec/frontend-interfaces.[ext]
          Four error types as discriminated union (tags match §7 exactly).
          Auth state type consistent with §8 token_delivery.
          One typed async function per endpoint — Result<T,E>, never throws.

        End with: "Plan Part C3c (interfaces) complete. Waiting for review."

  - id: review-plan-c3c
    type: gate
    message: |
      ━━ REVIEW PLAN PART C3c (interfaces) ━━

      [ ] backend-interfaces: signatures match §4? Zero infra imports?
      [ ] frontend-interfaces: Result<T,E> returns? Error tags match §7?
      [ ] frontend-interfaces: one typed function per §8 endpoint?

      Edit interfaces and revise if needed.
    options: [approve, revise]
    on_revise:
      goto: plan-part-c3c

  # ── PHASE 3C3D: GITIGNORE + CLAUDE.MD + CI-LOCAL.SH ────────────
  # Independent of each other — all downstream from plan.md.
  - id: plan-part-c3d
    command: speckit.plan
    integration: claude
    input:
      args: >
        §1–§20 of plan.md are approved. Produce ONLY:
          .gitignore
          .env.example
          CLAUDE.md
          scripts/ci-local.sh

        .gitignore (copy from boilerplate and verify):
          The boilerplate .gitignore covers common patterns.
          After copying, verify that any project-specific build output
          directories, generated files, or secret file patterns are added.
          The .env file must be listed. Only .env.example is committed.

        .env.example (create from project-brief.md):
          List every environment variable the project needs, with placeholder
          values (no real secrets). Derive keys from:
            plan.md §14 local_dev_setup.environment_variables
            any infrastructure URLs from project-brief.md
          Format: KEY=placeholder_value (one per line, no blank lines between entries).
          Commit this file. Do NOT commit .env or .env.local.

        CLAUDE.md (update, max 150 lines):
          Use ddd-clean-arch/templates/claude-md-template.md as the structure.
          Fill in every placeholder from plan.md:
            [PROJECT_NAME]       → from project-brief.md Project name
            What this is         → from §1 requirements scope + out_of_scope
            Stack                → from project-brief.md stack fields + §13 api_testing_tool + e2e_testing_tool
            Architecture         → from §6 modules + future_extraction_candidates
            Module boundaries    → from §6 modules (owns + does_not_own per module)
            Ubiquitous language  → from §2 + §4 (aggregate roots, events, value objects, repos)
            What NOT to do       → from §16 verbatim (all 10 constraints, do not paraphrase)
            Test conventions     → from §13 (all frameworks, locations, runners)
            Regression commands  → from §13 regression_command (all three)
            Definition of done   → from §17 verbatim
          The output must be ≤150 lines. If it exceeds 150 lines, condense the
          Architecture and Module boundaries sections — never cut §16 or §17.

        scripts/ci-local.sh (fill in the COMMANDS section):
          Populate the command variables at the top of scripts/ci-local.sh
          using the values from plan.md §13 and §14:
            SECRET_SCAN_CMD: gitleaks detect --source . --redact -q
            LINT_CMD: from §14 task_runner.lint
            ARCH_TEST_CMD: from §20 backend_arch_tool runner + frontend arch:check
            UNIT_TEST_CMD: from §13 regression_command.api_only (includes unit tests)
            INTEGRATION_TEST_CMD: from §13 regression_command.api_only (includes integration tests)
            API_TEST_CMD: from §13 api_testing_tool.runner
            CONTRACT_TEST_CMD: from §13 regression_command.api_only (or leave empty if not applicable)
            E2E_TEST_CMD: from §13 regression_command.e2e_only

        End with: "Plan Part C3d (gitignore, CLAUDE.md, ci-local.sh) complete. Waiting for final review."

  - id: review-plan-c3d
    type: gate
    message: |
      ━━ REVIEW PLAN PART C3d (gitignore, CLAUDE.md, ci-local.sh) ━━

      [ ] CLAUDE.md: ≤150 lines? All template sections filled? §16 verbatim?
      [ ] .gitignore: .env listed? Project-specific patterns added?
      [ ] ci-local.sh: all command variables populated from plan.md?
      [ ] §9: secret_scanning.ci_gate is yes? .gitleaks.toml referenced?
    options: [approve, revise]
    on_revise:
      goto: plan-part-c3d

  # ── PHASE 4: TASKS ─────────────────────────────────────────────
  - id: tasks
    command: speckit.tasks
    integration: claude
    input:
      args: >
        Read plan.md and use ddd-clean-arch/templates/tasks-template.md.

        Produce one task per aggregate (domain layer), one per aggregate
        (infra layer), one per endpoint group (api layer), then shared,
        then integration tasks, then frontend-data, frontend-feature, e2e —
        in that order.

        AFTER producing shared/frontend-data/frontend-feature/e2e tasks,
        read plan.md §3 (Bounded contexts). For each bounded context relationship
        (shared kernel, ACL, open host service, publisher-subscriber),
        produce an integration task:

          ## TASK-[N]
          Title: [Context A] → [Context B] integration
          Type: integration
          Depends on: [all domain + infra + api tasks for both contexts]
          Scope:
            Creates:
              - tests/integration/[contextA]_[contextB]_IntegrationTest.[ext]
            Modifies:
              - none
          Acceptance criteria:
            - [Test verifies that an event/command from Context A correctly
              triggers the expected behavior in Context B through the ACL/shared kernel]
          Do NOT:
            - [Do not implement domain logic — only write the integration test]

        Task order must be:
          1. backend-domain → 2. backend-infra → 3. backend-api →
          4. shared → 5. integration (cross-context) →
          6. frontend-data → 7. frontend-feature → 8. e2e

        Rules:
          - Acceptance criteria must name exact classes from plan.md.
            Form: "calling [ExactClass].[method]([input]) raises/returns [output]"
          - Each task has exactly one "Do NOT" scope-creep guard.
          - Max 5 files and max 1 aggregate per task.
          - All Depends-on fields must reference earlier tasks.
          - No circular dependencies.

  - id: review-tasks
    type: gate
    message: |
      Review .specify/specs/[feature]/tasks.md

      [ ] Task order: domain → infra → api → shared → integration → frontend-data → frontend-feature → e2e
      [ ] Integration tasks: one per bounded context relationship (shared kernel, ACL, publisher-subscriber)?
      [ ] Acceptance criteria verifiable (name exact classes/methods from plan.md)?
      [ ] Each "Do NOT" guard is specific to that task?
      [ ] No task touches more than 5 files or more than 1 aggregate?
      [ ] All Depends-on references are to earlier tasks?

      Approving proceeds to task dependency graph validation before implementation.
    options: [approve, revise]
    on_revise:
      goto: tasks

  # ── PHASE 4.5: TASK DEPENDENCY GRAPH VALIDATION ────────────────
  # Verify the task graph before starting implementation.
  # Catches circular dependencies, missing references, and ordering issues.
  - id: validate-tasks
    type: shell
    command: "bash scripts/validate-tasks.sh"

  - id: review-validate-tasks
    type: gate
    message: |
      Review the task dependency graph validation results above.

      ERRORS must be fixed before implementation starts:
        - Circular dependencies
        - References to non-existent tasks

      WARNINGS are advisory:
        - Task ordering issues (depends-on appears after dependent task)

      If there are errors, select "revise" to fix tasks.md.
      If clean, select "approve" to start implementation.
    options: [approve, revise]
    on_revise:
      goto: tasks

  # ── PHASE 5: IMPLEMENT LOOP ─────────────────────────────────────
  - id: implement_loop
    type: while
    condition: "{{ steps.check_remaining.output.has_todo == 'true' }}"
    steps:

      - id: check_remaining
        type: shell
        command: "bash scripts/check-tasks.sh"

      - id: check_in_progress
        type: if
        condition: "{{ steps.check_remaining.output.in_progress != '' }}"
        then:
          - id: resolve_in_progress
            type: gate
            message: |
              ⚠ An IN_PROGRESS task was detected from a previous interrupted session:
                {{ steps.check_remaining.output.in_progress }}

              A previous session was interrupted before this task completed.
              The task may have partial files on disk.

              Choose how to proceed:
                restart  — Mark the task TODO and restart it cleanly.
                          Recommended if the partial files should be discarded.
                continue — Leave it IN_PROGRESS and attempt to resume.
                          Use only if you know what was completed in the last session.
                abandon  — Mark it ABANDONED and skip to the next TODO task.
                          Use if the partial work is unrecoverable.
            options: [restart, continue, abandon]
            on_restart:
              type: shell
              command: >
                sed -i 's/^Status: IN_PROGRESS$/Status: TODO/' "{{ steps.check_remaining.output.feature_dir }}/tasks.md"
            on_continue:
              type: noop
            on_abandon:
              type: shell
              command: >
                sed -i 's/^Status: IN_PROGRESS$/Status: ABANDONED/' "{{ steps.check_remaining.output.feature_dir }}/tasks.md"

      - id: check_abandoned
        type: if
        condition: "{{ steps.check_remaining.output.abandoned_count | int > 0 }}"
        then:
          - id: warn_abandoned
            type: gate
            message: |
              ⚠ {{ steps.check_remaining.output.abandoned_count }} ABANDONED task(s) found.
              These tasks were interrupted and left with partial files on disk.
              Review them in tasks.md and clean up any partial files before continuing.
              Approve to continue with the next TODO task (ABANDONED tasks are skipped).
            options: [approve]

      - id: should_retrospect
        type: if
        condition: >
          {{ steps.check_remaining.output.done_count | int > 0 and (
             (steps.check_remaining.output.done_count | int >= (steps.check_remaining.output.first_retro_threshold | int) and
              steps.check_remaining.output.done_count | int % (steps.check_remaining.output.retro_interval | int) == 0) or
             (steps.check_remaining.output.done_count | int == steps.check_remaining.output.first_retro_threshold | int)
           ) }}
        then:
          - id: run_retrospect
            command: speckit.retrospect
            integration: claude

          - id: review-retrospect
            type: gate
            message: |
              Review the retrospect report.
              Respond to all MUST DO items before continuing.
              Confirm or reject each proposed spec change.
              Confirm or reject each proposed edge-case task addition.
              Approve when all blocking issues are resolved.
            options: [approve]

      - id: implement_one
        command: speckit.implement
        integration: claude
        input:
          args: >
            Run with --parallel flag to batch independent tasks.
            In parallel mode: find all TODO tasks whose dependencies are DONE,
            group them by dependency level, and implement each task in the
            same batch sequentially. After the batch, report completion.
            In single-task mode (no --parallel): implement only the first TODO task.
            Always check Depends-on before starting.

      - id: review-implement
        type: gate
        message: |
          Review the completion report for the last task.

          [ ] All acceptance criteria satisfied (test name + file:line references)?
          [ ] All 11 checks passed (arch, new tests, regression, lint, dep scan, migration, observability, browser, secret scan, performance budget, contract)?
          [ ] Check [C] regression: zero new failures in existing test suite?
          [ ] Check [J] performance budget: within §10 budget?
          [ ] Check [K] contract enforcement: API matches api-contract.yaml?
          [ ] Test file committed alongside implementation?
          [ ] Spec learnings reviewed — confirmed or rejected each proposed change?

          Approve to continue to the next task.
          Stop to pause the workflow (resume with: specify workflow resume <run-id>)
        options: [approve, stop]
        on_stop: abort

  # ── PHASE 6: FINAL VERIFY ───────────────────────────────────────
  - id: final_verify
    command: speckit.verify
    integration: claude
    input:
      args: "full"

  - id: review-final-verify
    type: gate
    message: |
      Review the final verify report.

      CLEAN → spec and code are fully aligned. Ready to merge.
      DRIFT DETECTED → fix issues before merging.
        For each issue, decide: fix in code (align to spec) or fix in spec
        (intentional deviation). Add fix tasks to tasks.md if needed,
        then select "fix-needed" to resume the implement loop.
    options: [approved-clean, fix-needed]
    on_fix-needed:
      goto: implement_loop
