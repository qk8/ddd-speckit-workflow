# ── PHASE 5 SUB-PHASE: STAGNATION GATE ──────────────────────────
# DEPRECATED: Stagnation gate logic moved into 05-implement.yml
# (Fix 6: gate must run immediately after check, not at end of loop body.)
#
# This file is retained for backward compatibility with build-workflow.sh
# sub-phase discovery. It contains no steps — the gate is defined inline
# in the main implement loop.

steps:
