# Project Brief

## Project name
[Single word or short slug, e.g. "invoiceflow"]

## What does this system do?
[2-5 sentences. What it does, who uses it, what it does NOT do.]

## Users and scale
[Who uses it? Estimated user count? Daily transaction volume?
Peak load estimate? Availability requirement?]

## Stack
backend_language: [e.g. Java]
backend_framework: [e.g. Spring Boot]
database: [e.g. PostgreSQL]
frontend_language: [e.g. TypeScript]
frontend_framework: [e.g. React]
monorepo_tool: [e.g. pnpm workspaces + Turborepo]

## Hard constraints
[Non-negotiable constraints. Examples:
- Integration with an existing legacy API required
- Specific auth provider must be used
- GDPR compliance required
- Specific deployment environment]

## What is explicitly OUT of scope
[Things this project will NOT do. Draw a clear boundary.]

## Known external integrations
[External systems and services this system will communicate with.]

## Complexity
[simple | medium | complex]
simple — small project (< 30 tasks), single team, low risk
medium — moderate project (30-80 tasks), single team
complex — large project (80+ tasks), multiple teams, high availability required
(defaults to medium if not specified)

## Security posture
[How sensitive is the data? Which compliance requirements?
Rate limiting, audit log, etc. needed?]
